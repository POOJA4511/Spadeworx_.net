using System;
using System.Collections.Generic;
using System.Text;

namespace Assignment4
{
   /* public delegate void calDelegate(int no1, int no2);
    class Delegates
    {
        static void Main()
        {
            DelegateTest dt = new DelegateTest();
            calDelegate dl = (dt.add);
            calDelegate dk = (dt.sub);
            calDelegate dm = (dt.mul);
            calDelegate dn = (dt.div);

            calDelegate cc = dl + dk + dm +dn;
            Console.WriteLine(cc);

            cc.Invoke(10,20);
        }
    }
    class DelegateTest
    {
        public void add(int x, int y)
        {
            Console.WriteLine( x+y);
        }
        public void mul(int x, int y)
        {
            Console.WriteLine(x * y);
        }
        public void sub(int x, int y)
        {
            Console.WriteLine(x - y);
        }
        public void div(int x, int y)
        {
            Console.WriteLine(x / y);
        }

    }*/
}

